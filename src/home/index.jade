doctype html
html
    head
        meta(charset="utf-8")
        title test
    body
        div.everything
            div.head
                template 连接到 {{url}} 来添加测试机器
            div.list
                div
                    |当前已连接测试机:
                    span.count 0
                ul
                    template
                        li {{name}}


        script(src="../../bower_components/requirejs/require.js")
        script(src="config.generate.js")

        // startup
        script.
            require.config({
                baseUrl: '../../'
            })
            require(['src/home/index'], function (index) {
                index.init()
            })